# RED evidence

1. Comparison: `node --test test/remove-background-enhancements.test.js` — failed because `#compare-button` was absent (0 passed, 1 failed).
2. Brush hardness/feather: focused enhancement suite failed at module load because soft-brush/pure helper contract was not implemented (`normalizeAspectCropRect` export absent while introducing the next vertical test); brush test expected feathered alpha unavailable.
3. Aspect presets: same focused RED run failed because `normalizeAspectCropRect` was not exported.
4. Custom color: same focused RED run failed because `parseHexColor` was not exported.
5. Edge cleanup: focused tests required `composeCroppedPixels(..., {cleanup})`; implementation did not accept effect options.
6. Adjustments: focused controls test required `#adjustments-panel`, zero-valued sliders/reset, and `reviseEffects`; all were absent before production edits.
7. Chromium behavior: `npm run build:remove-background && node --test test/remove-background-enhancements-browser.test.js` failed because the reset action was inaccessible while the adjustments details/fieldset were closed/disabled, then the MIME-aware server exposed the real handlers and the test passed.
